const coinListController = require('./coinList.controller');
const exchangeRateController = require('./exchangeRate.controller');

module.exports = {
  coinListController,
  exchangeRateController,
};
